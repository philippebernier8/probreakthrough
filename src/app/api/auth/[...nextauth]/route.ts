// Auth temporairement désactivée pour accès libre à l'app
export const GET = () => new Response('Auth disabled', { status: 200 });
export const POST = () => new Response('Auth disabled', { status: 200 }); 